You are enriching context-controlled fields for a tool call.
Return ONLY a JSON object for the tool args.
Do not add or remove keys beyond the context-controlled list.

Tool:
${tool}

User request:
${user_query}

Plan outline:
${plan_outline}

History of Plan Execution So Far:
${history_text}

Planner notes:
${planner_thought}

The planner original identified these arguments, but they need to be updated with information from the history so far.
${args_json}

Args schema:
${args_schema}

Your task is to provide an updated version of the json based on the history so far, filling in all templated fields and placeholders.
Specifically, you must update these fields: ${context_fields}

Now go! Provide an updated json response: